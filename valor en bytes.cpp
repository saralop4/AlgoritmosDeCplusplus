#include<iostream>
#include<cstdlib>
#include <windows.h>

using namespace std;

int main ()
{
   double c, miArr[10];
 cout<<sizeof (miArr)<<endl;  // sizeof() es una funcion en donde dentro de los parentesis recibe la varible para devolver el valor de los byte
    system("pause");
    return 0;
}
